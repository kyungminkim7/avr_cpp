#pragma once

#include <stdint.h>

#include <avr_cpp/usart.h>

namespace avr_cpp {

class Usart${n} {
public:
    Usart${n}(Usart::DataSize dataSize, Usart::NumStopBits numStopBits, 
              unsigned long baudRate,
              unsigned int baudTolerancePercent = Usart::DEFAULT_BAUD_TOLERANCE_PERCENT,
              unsigned long systemClockFreq = Usart::DEFAULT_SYSTEM_CLOCK_FREQ);
    ~Usart${n}();

    uint8_t receiveByte();
    void sendByte(uint8_t data);
};

} // namespace avr_cpp